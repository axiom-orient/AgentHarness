name: pr-review-template

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  summarize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Print review template reminder
        run: |
          echo "TODO: wire first-pass agent review or summary generation"
